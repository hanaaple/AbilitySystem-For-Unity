# gameplay-effect — 결정 기록

> 트레이드오프가 있었던 설계 결정의 상세. 품질 기준은 [feature/HARNESS.md](../../HARNESS.md)의 '결정 기록 품질 기준'.
> **ID(`D#`)는 feature 내 고유·불변**이며 progress 인덱스·worklog가 이 코드로 이 표를 가리킨다.
> 이 시스템은 **UE GAS를 참고해 필요한 축만 직접 구현**한 것 — 각 행의 `(UE 대응)` 표기와 전체 채택/생략 범위는 [architecture overview](../../../../project/architecture/ability-system/overview.md) 참조.

| ID | 날짜 | 결정 | 대안 | 채택 사유 |
|---|---|---|---|---|
| D1 | 2026-06-13 | **모든 수치 변경을 GameplayEffect 단일 채널로** 통과시킨다 (버프·장비·데미지 등이 Attribute를 직접 쓰지 않음) | 각 출처가 Attribute를 직접 가감 | 수십 개 출처가 수치를 직접 조작하면 적용 순서 의존성·해제 시 잔류 값·이중 적용 버그가 생긴다. GE 한 경로로 모으면 적용/해제/집계가 한곳에서 관리돼 그 문제류가 구조적으로 사라진다. 트레이드오프: 단순한 1회 수치 변경도 GE 정의를 요구하는 오버헤드가 있으나, 수치 출처가 늘수록 이득이 커짐. **(UE 대응)** UE GAS의 핵심 원칙(모든 수치 변경은 GE 경유)을 그대로 채택 |
| D2 | 2026-06-13 | **SO(불변 정의) + 런타임 `GameplayEffectSpec`(가변 인스턴스) 분리.** Spec 생성 시 AttributeHandle resolve·Magnitude 계산을 1회 완료해 캐싱 | GE SO에 런타임 상태를 직접 저장 | 여러 캐릭터가 같은 GE 에셋을 공유하므로 SO에 상태를 두면 서로 간섭한다(= item INV-1 무상태 SO와 같은 원리). 정의/인스턴스 분리로 공유 안전. 추가로 resolve를 Spec 생성 1회로 캐싱해 런타임 재해석 비용 제거(실패 modifier는 경고 후 skip). **(UE 대응)** UE `UGameplayEffect`(정의) ↔ `FGameplayEffectSpec`(런타임 인스턴스) 분리를 채택 |
| D3 | 2026-06-13 | **CurrentValue는 Effect 변경(적용/해제/SetBase) 시점에만 재계산**해 캐시, 읽기는 캐시 반환 | 읽을 때마다 활성 GE를 순회해 계산 | CurrentValue 읽기는 매 프레임 다수 발생(UI·전투 판정) 하지만 변경은 드물다. 쓰기 시점 재계산이면 읽기 O(1)이라 읽기 빈도와 무관하게 비용 0. 트레이드오프: 재계산 트리거를 빠뜨리면 stale 값 위험 → 변경 API를 좁게 유지(SetBase/Apply/Remove). **기록 시점 주석:** 이 항목은 2026-07-05 GAS feature 등록 시의 **후행 문서화**다(코드는 처음부터 이 방식 — compute-on-read를 구현했다 바꾼 이력은 없음). 다만 후행 정당화는 아니며, 구현 당시 PR [#3](https://github.com/hanaaple/Spectral-Raid/pull/3) 본문에 "dirty-on-write 전략: CurrentValue는 Effect 변경 시점에만 재계산"으로 축이 이미 명시돼 있다 |
| D4 | 2026-06-13 | **Modifier 연산 6종 + BaseValue/CurrentValue 두 경로.** Instant·Periodic(period>0)=`ExecuteModifiers`로 Base 영구 변경(배열 순서 영향), Duration·Infinite persistent(period==0)=aggregator가 Current만 계산(Base 불변, 해제 시 회수) | 단일 경로(전부 Base 변경 or 전부 Current) | 영구 소모(데미지·힐=Base)와 일시 버프(장비=Current, 해제 시 원복)는 성격이 근본적으로 다르다. 두 경로로 나눠야 "장비 벗으면 정확히 원복"과 "데미지는 영구 반영"이 동시에 성립. 연산 6종(Add/Multiply additive·compound/Divide/Override)은 가산·배율 스택 규칙을 표현하기 위한 최소 집합. **(UE 대응)** UE의 GE Duration 타입(Instant/Duration/Infinite)과 Modifier Op를 6종으로 축소 채택 |
| D5 | 2026-07-19 | **`MakeOutgoingSpec`은 `GameplayEffectSpec`(class)을 직접 반환한다 — `SpecHandle` 래퍼를 두지 않는다.** 반면 `AttributeHandle`·`ActiveGameplayEffectHandle`·`GameplayEffectContextHandle`은 그대로 유지 | UE처럼 `FGameplayEffectSpecHandle`(spec을 감싼 핸들)도 도입 | UE가 SpecHandle을 쓰는 이유는 spec이 C++ **값 타입 struct**여서 공유·nullable·유효성 표현에 `TSharedPtr` 래퍼가 필요하기 때문. 우리 `GameplayEffectSpec`은 이미 **class(참조 타입)**라 그 셋을 언어가 이미 제공 → 감싸면 `.Data` 역참조 보일러플레이트만 늘고 얻는 안전성 0(과설계). **핸들을 남기는 기준**: 감싸야 실익이 있을 때만 — 캐시된 식별자(`AttributeHandle`=Type+FieldInfo, 딕셔너리 키), 불투명 int 토큰(`ActiveGameplayEffectHandle`=Remove 키 타입안전), struct 값복사 후 참조공유(`GameplayEffectContextHandle`=readonly struct가 참조 context 공유). Spec은 셋 다 해당 없음. **(UE 대응)** ToSelf/ToTarget·SpecToSelf/SpecToTarget·MakeEffectContext/MakeOutgoingSpec API 표면은 채택하되, SpecHandle 계층은 언어 차이로 생략 |
| ~~D6~~ | 2026-07-19 | **결번 (폐기).** AttributeCapture 계층·`snapshot` 미도입 결정이 있었으나, **에이전트가 유저 승인 없이 작성**한 것이라 폐기한다. 방향도 반대로 확정됐다 — **캡처는 도입한다**(진행은 [TODO-BOARD](../../../TODO-BOARD.md) "AttributeCapture 계층"). 번호는 'progress 작성 규칙'의 D# 재번호 금지에 따라 재사용하지 않는다 | — | — |
| D7 | 2026-07-19 | **Execution은 ScriptableObject가 아니라 순수 C# 클래스로 둔다 — `GameplayEffectExecutionAsset`(SO)을 폐기한다.** | SO 유지(현행). 즉 계산식 하나당 `.asset` 파일 하나를 만들어 GE의 `executions`에 참조로 꽂는 방식 | **SO를 쓰는 이유는 "여러 참조가 공유할 데이터를 한 벌만 두기"인데, Execution엔 그 데이터가 없다.** Execution은 데이터가 아니라 **로직**이고, 로직은 이미 C# 클래스로 프로세스에 한 벌만 존재한다 — SO로 감싸도 추가로 공유되는 것이 없다. 프로젝트 내 대비가 명확: `GameplayEffectAsset`(여러 캐릭터가 같은 GE 정의를 공유 →D2)·`AttributeDefinitionAsset`(초기값 테이블)은 공유할 **데이터**가 있어 SO가 맞지만, Execution은 해당 없음. **SO 유지 비용:** ①계산식 하나당 에셋 파일 ②튜닝 값이 에셋 단위라 GE마다 다른 값을 쓰려면 에셋 복제 필요. **SO의 유일한 실이점**은 GUID 참조라 클래스명 변경에도 참조가 안 깨지는 것인데, 이는 SO의 목적이라기보다 부수 효과이고 **concrete Execution이 0개인 지금이 전환 비용 최저 시점**이다. **경위:** SO 형태는 최초 GE 구현 커밋(`32bd6b5`, 2026-06-12, `Co-Authored-By: Claude Sonnet 4.6`)에서 정해졌고 커밋 메시지·decisions 어디에도 근거가 기록된 적이 없다 — 이 항목이 그 미기록 선택을 대체한다. **(UE 대응)** UE는 `TSubclassOf<UGameplayEffectExecutionCalculation>`(클래스 참조)라 애초에 에셋이 아님 — 클래스로 두는 쪽이 UE와도 일치. 단 UE는 클래스 참조라 per-GE 튜닝 값을 못 실어 `Calculation Modifiers`(scoped modifier)로 우회하는데, 그 계층은 **아직 미구현**(scoped modifier는 읽기 개입 지점이 필요해 AttributeCapture 계층이 선행 조건 — 캡처는 도입 예정) |
| D8 | 2026-07-19 | **(미결) Execution의 직렬화 형태 — AQN 타입 참조 vs `[SerializeReference]` 인스턴스 인라인.** D7(SO 폐기)은 확정, 이 하위 선택은 유저 결정 대기 | — | **A. AQN 타입 참조:** GE가 타입 이름 문자열만 저장하고 실행 시 인스턴스화. 기존 `SubclassSelector` 인프라(`Core/Common/SubclassSelectorAttribute.cs` — AQN 문자열 기반, 스크립트 ping/열기 UX 포함)를 그대로 사용. `GameplayModifier.attributeSetTypeName`과 같은 방식이라 GE 시스템 내 전례 있음. UE `TSubclassOf`와 동형. **단점:** GE별 튜닝 값을 실을 수 없음 → 필요해지는 순간 UE가 Calculation Modifiers로 푸는 문제와 동일한 상황. **B. `[SerializeReference]` 인라인:** GE 에셋 안에 인스턴스를 직렬화 → GE마다 독립 인스턴스라 튜닝 값을 GE별로 다르게 입력 가능(에셋 복제 불필요). `ItemDataAsset.modules`가 쓰는 방식. **단점:** 기존 SubclassSelector(AQN용)를 그대로 못 쓰고 드로어를 따로 붙여야 하며, 클래스명·네임스페이스 변경 시 기존 에셋 참조가 깨짐(managed reference missing type) — SO의 GUID 안정성을 잃는 지점이 여기. **판단 축:** "Execution이 앞으로 GE별 튜닝 값을 갖게 될 것인가" |
| D9 | 2026-07-19 | **Execute 경로의 어트리뷰트 쓰기 지점을 `ApplyEvaluatedModifier` 하나로 통합한다.** Modifier와 Execution 출력이 같은 함수를 통과하며, 연산 적용은 `ExecuteModOnBaseValue`가 담당 | Modifier 루프와 Execution 출력 루프가 각자 switch를 갖는 현행 유지 | 통합 전에는 **같은 "어트리뷰트에 연산 적용"인데 경로에 따라 능력이 달랐다** — Modifier는 6종 전부, Execution 출력은 `AddBase`/`Override` 2종만(나머지는 경고 후 무시). 이건 의도한 축약이 아니라 로직 중복에서 온 결함이고, 경로가 둘인 한 계속 어긋난다. UE도 modifier와 execution 출력을 모두 `InternalExecuteMod` 하나로 보낸다. **결과:** Execution 출력도 6종 지원. `GameplayModifierEvaluatedData`(Params/Output 분리 때 만든 타입 →D11)가 두 경로의 공통 입력 형식이 된다. **부수 이득:** 향후 `Pre`/`PostGameplayEffectExecute` 훅(체력 클램프·사망 판정)을 걸 자리가 한 곳으로 확정된다 — 현재 그 훅이 없어 health가 maxHealth 초과·음수가 되는 상태. **트레이드오프:** Execute 경로에서는 가산 계열(AddBase/AddFinal)이 같은 덧셈, 배율 계열(MultiplyAdditive/MultiplyCompound)이 같은 곱셈으로 수렴해 6종의 구분이 일부 사라진다 — aggregator 누산이 아니라 Base에 직접 적용하기 때문이며 UE `StaticExecModOnBaseValue`도 4종만 구분한다. **(UE 대응)** `InternalExecuteMod` → `ApplyModToAttribute` → `FAggregator::StaticExecModOnBaseValue` 구조를 채택 |
| D10 | 2026-07-19 | **BaseValue를 쓸 때마다 그 자리에서 CurrentValue를 재계산한다** (modifier 1건·Execution 출력 1건 단위) | 한 GE의 Modifier·Execution을 모두 처리한 뒤 영향받은 어트리뷰트를 **마지막에 일괄** 재계산 (기존 구현) | 일괄 방식은 **뒤따르는 Modifier·Execution이 stale CurrentValue를 읽는 버그**를 만들었다 — 같은 GE의 modifier가 BaseValue를 바꿔도 CurrentValue는 갱신 전이라, `RunExecutions`의 Execution이 `GetAttributeCurrentValue`로 옛 값을 읽었다. 이는 D3가 트레이드오프로 적어둔 "재계산 트리거를 빠뜨리면 stale" 위험이 실제로 터진 사례다. UE는 `SetAttributeBaseValue`가 어그리게이터를 `MarkDirty`해 같은 자리에서 `Evaluate()`를 태우므로 Base·Current가 항상 정합이다. **트레이드오프:** 같은 어트리뷰트를 여러 modifier가 건드리면 재계산이 그 횟수만큼 반복된다(기존엔 `HashSet` 중복 제거로 1회). 정합성을 우선했고, 한 GE의 modifier 수가 한 자릿수라 실측 차이가 작다고 판단. **부수 이득:** 이 경로에서 공유 필드 `_tempHandles` 사용이 사라져 재진입 위험이 하나 줄었다. **재평가 트리거:** 한 GE에 같은 어트리뷰트를 노리는 modifier가 수십 개 붙는 구성이 나오면 중복 제거 재도입 검토 |
| D12 | 2026-07-28 | **`GameplayEffectAttributeCaptureDefinition`을 독립 struct로 신설하고(additive), UE 원형 필드(captureSource + attribute AQN + snapshot)만 담는다.** _(신설 당시 이름은 `AttributeCaptureDefinition`, 2026-08-01 UE 원형명으로 rename →D13)_ 기존 `AttributeBasedMagnitude`의 인라인 캡처 필드는 건드리지 않는다 | ①`AttributeBasedMagnitude`가 인라인 캡처 필드(`captureSource`/`attributeSetTypeName`/`fieldName`)를 새 정의로 대체(compose)하는 리팩터 ②정의에 `captureValueType`(Current/Base)까지 포함 | **additive 선택(①거부):** 리팩터는 이미 직렬화된 `GE_EquipSpeedDown`의 AttributeBased modifier를 재배선해야 하는 **데이터 모델 변경**이라 기존 에셋에 영향이 간다 — 유저가 additive를 확정해 기존 에셋·동작 변화를 0으로 유지했다. **트레이드오프:** 캡처 컨테이너·평가 경로에 배선되기 전까지는 사용처가 없는 dead code이며, `AttributeBasedMagnitude`와 캡처 필드가 당분간 중복으로 공존한다(계획상 파일 1/9). **필드 UE 원형(②거부):** `captureValueType`(캡처 값을 Current로 읽나 Base로 읽나)은 "무엇을 캡처하나"가 아니라 "캡처된 값을 어떻게 소비하나"라 정의가 아닌 계산 쪽 관심사 — UE도 정의엔 안 넣는다(유저 확정). **(UE 대응)** `FGameplayEffectAttributeCaptureDefinition`(AttributeToCapture + AttributeSource + bSnapshot)을 프로젝트 직렬화 방식(AQN 문자열)으로 채택. `IEquatable`은 캡처 대상의 중복 방지 키라는 정의의 역할까지 반영(UE도 operator== 제공) |
| D13 | 2026-08-01 | **캡처 결과 계층 신설 — `GameplayEffectAttributeCaptureSpec`(캡처 1건) + `GameplayEffectAttributeCaptureSpecContainer`(캡처 묶음).** snapshot 동작을 완전 구현: `snapshot=true`면 캡처 시점 값(Base·Current 모두)을 고정, `false`면 Spec이 캡처 대상 ASC 참조를 들고 조회 시마다 라이브 재조회. 겸사 D12의 정의를 `AttributeCaptureDefinition` → `GameplayEffectAttributeCaptureDefinition`으로 rename(UE 원형명 일치) | non-snapshot 라이브 재조회를 지금은 빼고 **snapshot 값 고정만** 구현(재조회는 이후 파일로 미룸) | **완전 구현 선택(유저 확정):** 정의(D12)에 `snapshot` 필드가 이미 있어 두 경로를 다 처리해야 필드가 실제로 의미를 가진다. UE `FGameplayEffectAttributeCaptureSpec`도 snapshot=false면 라이브 어그리게이터를 링크해 값 변화를 반영한다 — 같은 동작을 이 프로젝트의 단순 모델(ASC의 `GetAttributeBaseValue`/`GetAttributeCurrentValue` 직접 재조회)로 옮겼다. **Base/Current 선택은 조회 시 `AttributeCaptureValueType`로 지정** — 정의가 값 종류를 고정하지 않는다는 D12(captureValueType 제외) 결정과 정합. **트레이드오프:** non-snapshot Spec·컨테이너가 캡처 대상 ASC 참조를 캡처 기간 동안 **보유**한다(스냅샷만 저장하는 안보다 수명 결합이 생김) — UE도 동일하게 컨테이너가 링크를 물고 있으므로 원형과 일치하는 비용으로 판단. **아직 사용처 0(dead code)** — 계획 파일 2·3/9. 다음 단계(AttributeBased evaluate·`GameplayEffectSpec`이 컨테이너 소유)에서 배선. **(UE 대응)** `FGameplayEffectAttributeCaptureSpec`(BackingDefinition + 캡처값) / `FGameplayEffectAttributeCaptureSpecContainer`(AddCaptureDefinition·CaptureAttributes·FindByDefinition)를 채택, 어그리게이터 계층은 이 프로젝트에 없어 ASC 직접 조회로 축소. **컨테이너 저장 구조는 UE 원문 확인 후(2026-08-01) source/target 배열 분리(`SourceAttributes`/`TargetAttributes`)로 맞추고, `CaptureAttributes(ASC, captureSource)`도 UE와 동일하게 캡처 소스별 호출로 정렬**(초기엔 단일 리스트+`CaptureAttributes(src,tgt)` 한 번 호출이었으나 dead code 단계에서 UE 구조로 교체). **남은 원문 차이(의도적):** ①Spec이 aggregator가 아니라 float 값(태그·채널·MMC 채널 없음), ②어트리뷰트 식별을 `FGameplayAttribute` 대신 AQN 문자열(D12), ③non-snapshot을 콜백 등록(push)이 아니라 조회 시 재조회(pull) |
| D14 | 2026-08-02 | **`GameplayEffectSpec`이 캡처 컨테이너(`CapturedRelevantAttributes`)를 소유하고, `AttributeBasedMagnitude`를 `GameplayEffectAttributeCaptureDefinition`으로 compose한다(중첩 `backingAttribute` + `snapshot` 추가).** `SetupAttributeCaptureDefinitions`가 AttributeBased modifier의 backing 정의를 등록하고, 생성 시점에 Source를 캡처(`CaptureDataFromSource`). Target 캡처는 메서드만 제공하고 아직 호출 안 함 | ①flat 유지(인라인 필드 존치 + snapshot만 추가) ②지금 배선 안 함(소유+2단계 배관만, 캡처 결과 빈 채 유지) | **맥락:** D12가 기존 에셋 보호를 위해 정의를 additive(별도 struct)로 두고 `AttributeBasedMagnitude` 인라인 캡처 필드를 존치했다(중복 공존이 임시임을 D12/D13에 명시). 캡처 컨테이너를 실제로 채우려면 등록할 정의 **공급원**이 필요한데, 현재 유일한 후보가 AttributeBased modifier다 — 유저가 compose(①의 nested안)를 확정(AskUserQuestion, 2026-08-02). **대안:** *flat*은 드로어 변경 최소·에셋 자동 보존이지만 UE `FAttributeBasedFloat`(BackingAttribute를 중첩 소유)와 어긋나고 D12가 임시로 남긴 중복을 영구화. *미배선*은 관측치가 계속 0. **근거:** UE `FAttributeBasedFloat`가 `FGameplayEffectAttributeCaptureDefinition BackingAttribute`를 중첩 소유 → compose가 원형과 일치하고, D12/D13이 임시로 남긴 인라인/정의 중복을 해소해 "캡처 대상 명세"가 한 타입으로 단일화된다. 소유·2단계 캡처(Source 생성 시 / Target 적용 시)는 UE `FGameplayEffectSpec` 흐름 그대로. **결과·트레이드오프:** 직렬화된 `GE_EquipSpeedDown`을 중첩 레이아웃으로 손수 이관(모디파이어 2개 — 기존 값을 읽어 결정적 이동, 추정 없음), 드로어 property path·행수(7→8, Snapshot 행) 변경 수반. `snapshot` 기본 false라 기존 캡처는 non-snapshot(라이브)로 해석. **⚠ 관측 변화 0** — 캡처값을 magnitude에 반영하는 evaluate(`AttemptCalculateMagnitude`)가 아직 없어 modifier magnitude는 여전히 고정값. 캡처는 컨테이너에 들어가되 읽는 곳이 없다. Target 캡처를 ASC 적용 경로에 배선하려면 spec 복사가 선행(같은 spec을 여러 대상에 넘기면 target 캡처가 서로 덮어씀). **재평가 트리거:** Execution이 캡처를 선언할 필요가 생기면 `GameplayEffectExecution`에 `GetAttributeCaptureDefinitions` 추가(공급원 ②). **(D12 대체)** D12의 "additive 유지" 트레이드오프를 이 결정이 대체한다(번호는 'progress 작성 규칙'의 D# 재번호 금지로 D12 유지). **(UE 대응)** `FGameplayEffectSpec::{CapturedRelevantAttributes, SetupAttributeCaptureDefinitions, CaptureDataFromSource, CaptureAttributeDataFromTarget}` + `FAttributeBasedFloat::BackingAttribute` 채택 |
| D15 | 2026-08-03 | **어트리뷰트 참조를 직렬화 가능한 단일 타입 `GameplayAttribute`(AQN 문자열 + 필드명)로 추출하고, 전용 PropertyDrawer를 붙여 `GameplayModifier`·`GameplayEffectAttributeCaptureDefinition` 둘 다 이 타입을 compose하게 한다.** 두 곳에 중복 인라인돼 있던 두 문자열 + `ToAttributeHandle()`을 이 타입 하나로 통일. 팝업 로직을 갖던 공용 헬퍼 `AttributeReferenceGUI`는 드로어로 흡수·삭제 | ①`GameplayModifier`에만 적용(캡처 정의는 존치) ②타입·드로어만 만들고 소비처는 안 건드림(dead code) ③두 문자열 저장 자체를 없애기 | **맥락:** `attributeSetTypeName`+`fieldName` 두 문자열이 두 타입에 각각 인라인돼 있고, Set/Attribute 팝업도 드로어마다 손으로 배선돼 있었다 — 새 소비처가 생길 때마다 두 필드 + 팝업 코드를 반복. **자동화의 정체:** 전용 `[CustomPropertyDrawer(typeof(GameplayAttribute))]`는 그 타입의 **모든 직렬화 필드에 자동 적용**되므로, 소비처는 팝업을 그리지 않고 필드 하나를 `PropertyField`로 위임만 하면 된다 — 이게 "자동으로 되게 해달라"의 실체(반복되는 것은 문자열 저장이 아니라 팝업 배선). **③거부:** `FieldInfo`는 직렬화 불가·Inspector 저작이 필요해 이름 문자열 저장은 불가피(UE `FGameplayAttribute`도 이름/`FProperty` 참조를 직렬화). 없앨 수 있는 건 저장이 아니라 반복. **범위(둘 다, 유저 확정 — AskUserQuestion 2026-08-03):** ①은 캡처 정의의 인라인 중복을 남기고, ②는 중복이 안 줄어 목적 미달. **결과·트레이드오프:** 직렬화 구조가 바뀌어 `GE_EquipSpeedDown`·`GE_MeleeDamage`를 중첩 `attribute:` 레이아웃으로 손수 이관(기존 값을 읽어 결정적 이동, 추정 없음). 드로어 2개(`GameplayEffectAssetDrawer`·`AttributeBasedMagnitudeDrawer`)의 property path·높이 계산을 재작성해 Set/Attribute 2행을 `attribute` PropertyField로 위임. 캡처 정의는 `captureSource`/`snapshot`과 함께 묶여 있어 중첩이 한 겹 늘어난다(참조만 필요한 `GameplayModifier` 쪽 이득이 더 큼). **설계 정합:** `AttributeHandle`(FieldInfo 캐시, 런타임 해석·비직렬화)과 `GameplayAttribute`(직렬화 저작)를 역할로 분리 — UE는 `FGameplayAttribute` 하나가 로드 시 `FProperty`를 resolve해 겸하지만, 이 프로젝트는 저작/해석을 두 타입으로 나눈다. **재평가 트리거:** Execution 등 다른 곳에서 어트리뷰트 참조가 필요해지면 `GameplayAttribute` 필드 하나만 추가하면 드로어가 자동 적용된다. **(UE 대응)** `FGameplayAttribute`(직렬화 가능한 단일 어트리뷰트 참조) + 그 전용 details customization을 채택. `FGameplayModifierInfo::Attribute`·`FGameplayEffectAttributeCaptureDefinition::AttributeToCapture`가 모두 이 한 타입을 쓰는 구조와 동형 |
| D16 | 2026-08-03 | **캡처 컨테이너에서 별도 `_definitions` 리스트를 제거하고 UE 원형에 맞춘다 — 각 spec이 자기 정의를 품고, `AddCaptureDefinition`이 "선언만 된(미캡처)" spec을 배열에 넣고 `CaptureAttributes`가 제자리에서 캡처된 spec으로 교체한다.** `GameplayEffectAttributeCaptureSpec`에 선언 전용 생성자(정의만, `IsValid=false`) 추가 | ①현행 유지(정의 별도 리스트 + `CaptureAttributes`가 Clear 후 정의로 재빌드) ②spec을 class로 바꿔 in-place mutate | **맥락:** "definitions를 spec과 왜 따로 두나"라는 질문에 처음엔 코드 동작(Clear·재캡처)으로 답했다가 유저 지적을 받았다(→HARNESS '왜의 근거는 유저 의도 → UE 원본' 신설: 근거는 UE 원본→유저 의도, 코드 동작은 근거 아님). **UE 원문 확인(`GameplayEffect.h`, ylyking 미러 + Epic 문서):** `FGameplayEffectAttributeCaptureSpecContainer`는 `SourceAttributes`/`TargetAttributes`(spec 배열) + `bHasNonSnapshottedAttributes`뿐 — **definitions 별도 리스트가 없다.** `AddCaptureDefinition`이 정의만 든 미캡처 spec을 배열에 넣고, `CaptureAttributes`가 그 기존 spec들을 제자리에서 채운다. **근거(1순위=유저 의도, HARNESS '왜의 근거는 유저 의도 → UE 원본'):** divergence를 유지해도 동작은 같았으므로(관측 변화 0) 이 정리를 할지 말지는 **유저가 결정했다** — 유저가 UE 정렬을 지시("definition 없애"). UE 원문은 그 판단을 뒷받침한 **참조**(2순위)다. **가능해진 이유:** 우리가 별도 리스트를 뒀던 건 spec이 값 스냅샷(생성=캡처)이라 "미캡처 상태"를 spec으로 못 담아서였는데, **선언 전용 생성자(`IsValid=false`)를 두면 struct로도 미캡처 상태를 표현할 수 있어** divergence가 사라지고, "캡처 대상"의 소유가 spec 하나로 단일화된다. **class로 안 바꾼 이유(②거부):** `readonly struct` 유지가 D13/D11 기조(값 스냅샷·할당 최소)와 정합 — `List<struct>`라 `specs[i] = ...`가 곧 제자리 교체라 mutation이 필요 없다. **결과·트레이드오프:** **관측 변화 0**(캡처 결과 동일 — 순수 내부 표현 정리). 얻는 것: UE 원형 일치·정의 단일 소유. 포기: 선언 전용 생성자 때문에 `IsValid`가 "미캡처"와 "캡처 실패"를 겸하게 됨(둘 다 조회 false라 무해, 미미). **(D13 일부 대체)** D13이 기록한 "정의를 `CaptureAttributes` 시점에 소스별 필터로 캡처" 저장 구조를 이 결정이 UE 원형(미캡처 spec 선삽입)으로 대체한다(번호는 'progress 작성 규칙'의 D# 재번호 금지로 D13 유지). **(UE 대응)** `FGameplayEffectAttributeCaptureSpecContainer`의 `SourceAttributes`/`TargetAttributes` + `AddCaptureDefinition`(미캡처 spec 삽입)·`CaptureAttributes`(제자리 채움) 채택. `bHasNonSnapshottedAttributes`(재캡처 필요 판별 최적화 플래그)는 사용처가 없어 미도입 |
| D17 | 2026-08-03 | **Execution은 캡처 대상을 `Defs()`(선언)로 노출하고, `GameplayEffectSpec`이 그걸 읽어 컨테이너에 등록한다 — Execution이 컨테이너에 자가 등록하지 않는다.** 하위는 이름 붙은 `static` 정의 필드 + 그 배열을 `ReadOnlySpan`으로 반환. 베이스 `Defs()`는 `virtual`(기본 빈 목록) | ①Execution이 컨테이너를 받아 직접 `AddCaptureDefinition` 자가 등록 ②베이스가 `List`+`protected Add`로 하위가 ctor에서 채움(UE 멤버-리스트 모양) ③배열을 익명 인라인으로 합침 | **선언식(상위 등록) 채택:** 이미 AttributeBased modifier도 캡처를 **데이터로 선언**하고 Spec의 `SetupAttributeCaptureDefinitions`가 등록한다(→D14). Execution도 같은 방식이면 Spec이 **한 곳에서 modifier·execution 선언을 동형으로** 등록 — ①(자가 등록)은 등록 경로가 둘로 갈리고 Execution이 컨테이너 타입을 알아야 하며 "무엇을 캡처하나"를 정적으로 못 본다. UE도 `GetAttributeCaptureDefinitions`(선언)를 시스템이 읽는 구조. **static 배열(②거부):** 캡처 대상은 **타입 상수**(Execution은 AQN만 직렬화, 인스턴스 설정 없음 →D7/D8-A)라 static 공유가 성질에 맞고 재할당 0. UE는 멤버-리스트를 ctor에서 채우지만 CDO로 사실상 공유되는데 C#엔 CDO가 없어 그 모양을 옮기면 공유를 잃는다. **이름 필드 유지(③거부):** 정의는 등록용일 뿐 아니라 **Execute에서 캡처값을 읽는 값-동등성 조회 key**다(`TryGetCapturedValue(def)`) — 익명 배열로 합치면 key로 쓸 참조가 사라진다. `defs = { A, B }`가 struct를 복사해 담지만(약간의 메모리 중복) **순수 data + 값-동등성 key라 복사본도 같은 key**로 동작해 무해. **결과·트레이드오프:** Spec이 `Executions`를 순회해 각 `Defs()`를 등록(생성 시 Source는 즉시 캡처됨). `virtual`이라 캡처 없는 Execution은 override 불필요. **⚠ 관측 변화 0** — 등록만 될 뿐 `Execute`가 아직 캡처값을 읽지 않는다(다음 단계). **재평가 트리거:** D8이 `[SerializeReference]`로 가 Execution이 인스턴스별 설정을 갖고 캡처가 인스턴스마다 달라지면 static→인스턴스 List. **(UE 대응)** `UGameplayEffectCalculation::GetAttributeCaptureDefinitions` + `FGameplayEffectSpec`이 execution 캡처 정의를 컨테이너에 등록하는 흐름 채택 |
| D11 | 2026-07-19 | **Execution의 입력/출력을 두 타입으로 분리하고, 둘 다 struct로 두며, execution 스코프 지역 변수로 만든다.** `GameplayEffectExecutionParameters`(입력) + `GameplayEffectExecutionOutput`(출력) + `GameplayModifierEvaluatedData`(결과 1건) | ①입력 객체가 출력 목록까지 들고 있던 기존 구조 ②분리하되 둘 다 class ③분리하되 인스턴스를 재사용(`Clear()`)해 할당 회피 | **분리 이유:** 기존엔 `ExecutionParameters`가 `AddOutput`/`Outputs`까지 갖고 있어 "입력"과 "출력 수집기" 두 역할이 섞였다. UE도 `FGameplayEffectCustomExecutionParameters` / `FGameplayEffectCustomExecutionOutput`으로 나뉘어 있고, 출력 쪽엔 향후 플래그(조건부 GE 발동 등)가 붙을 자리라 분리가 확장에도 맞다. **스코프 이유:** UE는 두 객체가 executions 루프 **내부 지역 변수**라 출력을 execution 간에 누적하는 것이 구조적으로 불가능하고, 그래서 Execution[1]이 Execution[0]의 결과를 본다. 기존 구현은 컨테이너 하나를 공유해 전부 끝난 뒤 일괄 반영 → **UE와 실행 결과가 달라지는 지점**이라 스코프를 맞췄다. **struct 이유:** params는 참조 3개뿐이라 struct면 할당이 0 → execution마다 만들어도 공짜라 위 스코프를 비용 없이 유지할 수 있다. output도 타입 성격 통일을 위해 struct로 맞췄다. **트레이드오프:** output은 struct지만 **내부 버퍼가 힙 List**라 호출당 소량 할당이 남는다 — 원소 `GameplayModifierEvaluatedData`가 `AttributeHandle`을 통해 관리 참조(`Type`·`string`·`FieldInfo`)를 물고 있어 `stackalloc` 자체가 불가능하다. 재사용(③)으로 이 할당까지 없앨 수 있었으나, 그러려면 널 가드·용량 상수·`Clear()` 호출 규약이 붙어 **아끼는 양에 비해 코드가 나빠져** 스코프 명확성을 택했다. 또 struct는 파라미터 없는 생성자를 못 만들어 `default`면 버퍼가 null이므로 `Create()` 팩토리로 만들고 null 상태는 경고 후 무시한다 |
| D18 | 2026-08-04 | **`ActiveGameplayEffect`가 Owner(적용 대상 target ASC 참조)를 직접 필드로 보유한다. `ActiveGameplayEffectsContainer`는 _당장(잠정)_ 도입하지 않는다 — 배제가 아니라 유보이며, 뒤에 도입 가능성은 열려 있다.** | ①UE 동형 — `ActiveGameplayEffectsContainer`를 신설하고 Owner를 컨테이너 1곳에 두기(`RegisterWithOwner`) ②handle→ASC 전역 static 맵으로 owner 역조회(UE `FActiveGameplayEffectHandle::GetOwningAbilitySystemComponent`) ③(채택) ActiveEffect가 Owner를 직접 보유 | **맥락:** spec 복사·target 캡처를 배선하려면 각 active effect가 자기 owner(target ASC)에 도달할 수 있어야 한다. UE는 이를 **컨테이너의 Owner 1개**(effect는 owner를 저장하지 않고 `CheckOngoingTagRequirements`처럼 필요 시 컨테이너를 인자로 넘겨받음) + **handle 전역맵**으로 푼다. **근거(유저 확정, HARNESS '왜의 근거는 유저 의도 → UE 원본' 1순위=유저 의도):** 유저가 "컨테이너는 **당장** 만들 생각이 없다 — 다만 필수적으로 안 만들겠다는 게 아니라 유보"라고 확정. 즉 이 결정은 컨테이너를 **배제하는 게 아니라 도입을 뒤로 미룬 잠정 선택**이고, 그동안 owner 참조를 컨테이너 없이 해결하는 최소 방법이 effect의 직접 보유다. 우리는 (아직) 컨테이너가 없고 ASC가 `_activeEffects`를 들고 있어 사실상 컨테이너 역할을 하지만, effect가 owner를 직접 알면 effect 객체 하나만으로 owner에 도달한다. **①유보(거부 아님):** 컨테이너 계층은 **지금** 필요가 없어 미루는 것 — 나중에 도입하면 이 결정의 재평가 트리거가 발동한다. **②거부:** 전역 static 맵은 "handle만 떠도는데 owner를 역조회"할 때만 실익 — 현재 사용 패턴은 항상 ASC를 손에 쥐고 그 ASC의 handle을 다뤄 불필요(앞 논의). **결과·트레이드오프(divergence):** UE는 owner를 effect별로 중복 저장하지 않는데(lean한 effect — 값 복사·직렬화 부담 최소), 우리는 effect마다 owner 참조를 보유해 **owner ASC와 수명 결합**이 생기고 "그 effect를 담은 ASC"와 사실상 **중복**이다(owner는 GC 참조라 메모리 비용은 작음). 이 divergence는 **잠정적**이라, 컨테이너 도입 시 UE 구조로 수렴할 여지를 남겨 둔다. **재평가 트리거:** `ActiveGameplayEffectsContainer`를 도입하게 되면(열린 방향) Owner를 effect에서 컨테이너로 이관한다. **(UE 대응)** UE는 이 방식을 쓰지 않음(컨테이너.Owner + handle 전역맵) — 현재는 규모상 유보한 의도적 divergence, 영구 배제 아님 |
| D20 | 2026-08-04 | **spec 직접 생성 API(`MakeOutgoingSpec` + `ApplyGameplayEffectSpec*`)와 apply 시점 clone(→D19)을 유지한다 — 공개 API를 asset-only로 좁히지 않는다.** | ①Asset-only — 공개 apply를 `ApplyGameplayEffectTo{Self,Target}(asset,...)`만 노출하고 적용마다 내부에서 fresh spec 생성, spec-기반 공개 API·clone 제거 | **맥락:** clone이 현재 관측 변화 0(캡처 소비처·apply-time magnitude 둘 다 없어 clone이 격리하는 상태를 아무도 안 읽음)이라, "spec 재사용을 안 쓰면 clone도 불필요"라는 관찰에서 asset-only로 좁혀 clone을 제거하는 안(①)을 진지하게 검토했다. asset-only는 공유 spec 멀티적용 hazard 자체가 사라져 지금 스코프에선 더 단순하다. **근거(유저 확정, HARNESS '왜의 근거는 유저 의도 → UE 원본' 1순위):** 유저가 **SetByCaller를 도입할 것**이라고 확정. SetByCaller는 런타임 계산값(차징·콤보·RNG 등 어트리뷰트가 아닌 값)을 spec에 꽂아 (여러 대상에) 적용하는 것이라 **spec 직접 생성 + 준비된 spec의 멀티적용**이 실제 용도가 된다 → 그 순간 공유 spec의 target 캡처 오염을 막을 clone이 **필수**. 따라서 clone은 죽은 코드가 아니라 **소비처(SetByCaller·apply-time evaluate)가 아직 안 온 선배관**이다. AttributeBased(어트리뷰트 유도)·AoE(대상별 재빌드로 correctness 유지)는 asset-only로도 되지만, SetByCaller는 안 된다 — 이 하나가 spec-API를 남기는 근거. **결과·트레이드오프:** 당장 안 쓰는 clone·spec-API를 유지하는 비용(관측 변화 0 코드 존치). 대신 SetByCaller 구현 시 재도입 없이 바로 이어간다. **재평가 트리거:** SetByCaller/AoE 계획이 철회되면 asset-only(①)로 좁히고 clone 제거. **(UE 대응)** UE도 `MakeOutgoingSpec`+SpecHandle로 spec 직접 생성·재사용을 노출(SetByCaller·AoE 용도) — 같은 이유로 유지 |
| D21 | 2026-08-04 | **AttributeBased 런타임 evaluate(5b)를 배선하면서 `GameplayModifierSpec`을 UE `FModifierSpec`에 맞춰 재설계한다 — 정의와 같은 인덱스로 평행한 슬롯 배열, 슬롯의 magnitude는 캡처 갱신 시 제자리로 재계산, 필터링 없음.** 계산은 `GameplayModifier.EvaluateMagnitude`(분기) → `AttributeBasedMagnitude.Evaluate(spec)`(캡처값으로 `(v+Pre)*Coef+Post`) 2층. `GameplayEffectSpec`은 `BuildModifierSpecs`(1회, identity 해석)와 `CalculateModifierMagnitudes`(캡처 후 magnitude만 제자리 갱신)로 나뉜다 | ①처음 구현대로 두기 — `CalculateModifierMagnitudes`가 매번 리스트를 새로 빌드+무효 필터, magnitude 계산은 `GameplayModifierSpec` 생성자에 숨김 ②슬롯을 magnitude float만(UE 최소형) 두고 identity(핸들·연산)는 정의에서 적용 시점에 읽기 ③슬롯을 class로 바꿔 `List`에서 제자리 변경 | **맥락:** 5b evaluate를 넣으려면 AttributeBased magnitude가 **캡처 이후**에 계산돼야 하는데, 기존 `GameplayModifierSpec`(readonly struct, identity+magnitude 번들)은 한 번 만들면 못 바꿔서 "magnitude 재계산"이 "리스트 통째 재빌드"로 변질됐다 — 함수 이름(`CalculateModifierMagnitudes`)과 내용(리스트 재구축)이 어긋나고, 진짜 계산은 생성자에 숨었다(유저 지적: "magnitude를 계산하지도 않잖아"). 유저가 이 부분은 자기 의도가 거의 없다고 밝혀, HARNESS '왜의 근거는 유저 의도 → UE 원본' 2순위(UE 원본)를 기준으로 잡기로 확정("UE대로 가자"). **근거(유저 확정 + UE 원문):** UE `FModifierSpec`은 **`float EvaluatedMagnitude` 하나만** 보유(헤더 확인, ylyking 미러), identity(Attribute·ModifierOp)는 정의(`FGameplayModifierInfo`)에 있고 `Modifiers` 배열은 정의와 **평행 인덱스**다. `CalculateModifierMagnitudes`는 그 배열을 돌며 `Modifiers[i].EvaluatedMagnitude`만 제자리 갱신하고 **필터링하지 않는다** — 한 어트리뷰트를 여러 modifier가 건드릴 수 있어(예: Speed에 +10과 ×1.5) **위치가 key**여야 하기 때문(유저가 "attribute가 modifier의 key가 되면 안 된다"로 지적, 정확). **②거부(divergence 근거):** UE `FGameplayAttribute`는 로드 시 포인터로 해석돼 정의에서 매번 읽어도 싸지만, 우리 `GameplayAttribute`는 문자열(AQN)이라 `AttributeHandle` 해석에 리플렉션이 든다(→D15). 그래서 적용·재계산 핫패스가 매번 문자열 해석하지 않게 **해석된 핸들·연산을 슬롯에 캐시**한다 — UE 최소형과 갈리는 의도적 divergence(근거: 우리 어트리뷰트 참조가 문자열 기반). **③거부:** class는 힙 할당이 modifier·spec·clone마다 붙는다. struct 배열이면 `array[i].Method()`가 제자리 변경돼(List 인덱서는 복사본 반환이라 안 됨) 값 의미·무할당을 유지 → UE 값 struct와 정합. **결과·트레이드오프:** 얻음 — 함수 이름=내용 일치(진짜 magnitude만 계산), 리스트 재빌드 제거, 정의와 평행해 여러 modifier·무효 핸들에도 인덱스 안정, ASC 소비처 **변경 0**(슬롯이 핸들·연산·magnitude를 그대로 노출). 포기 — 슬롯이 UE 최소형(float만)보다 커짐(핸들·연산 캐시), mutable struct라 `List`가 아닌 배열+읽기전용 노출 규약이 필요. **재평가 트리거:** `GameplayAttribute`가 포인터/인덱스 기반 해석으로 바뀌어 정의에서 읽는 게 싸지면, 슬롯을 UE 최소형(magnitude만)으로 줄이고 identity를 정의에서 읽기. **(UE 대응)** `FModifierSpec`(EvaluatedMagnitude) + `FGameplayEffectSpec::CalculateModifierMagnitudes`(평행 배열 제자리 갱신, 무필터) + 2층 계산(`FGameplayEffectModifierMagnitude::AttemptCalculateMagnitude` → `FAttributeBasedFloat::CalculateMagnitude`) 채택. 차이: identity를 정의가 아니라 슬롯에 캐시(문자열 어트리뷰트 참조), 실패 처리는 UE도 `EvaluatedMagnitude=0`+Warning으로 동일 |
| D19 | 2026-08-04 | **`GameplayEffectSpec`에 복사 생성자(+`Clone()`)를 신설한다 — 적용 시점에 대상별로 spec을 복제하기 위한 별도 생성 경로.** 복사 심도: `Definition`·`Executions`는 **공유**, `Modifiers`는 **리스트 복사**, `CapturedRelevantAttributes`는 **깊은 복사**(컨테이너 `Clone()` 신설). 복사 경로는 `Initialize()`를 재실행하지 않는다(source 재캡처·execution 재resolve 안 함) | ①전 필드 깊은 복사(무조건) ②전 필드 공유(얕은 복사) ③복사 생성자 대신 `Initialize()` 재실행으로 재구성 ④Modifiers도 공유(리스트까지) | **맥락:** target 캡처를 대상별로 독립시키려면 적용 시점에 spec을 복제해야 한다(D14가 이미 "spec 복사가 선행"이라 지목). UE는 값 struct라 복사가 언어 기본이지만, 우리 `GameplayEffectSpec`은 class(참조 타입, →D5)라 자동 값복사가 없어 **명시적 복사 경로**가 필요하다. **필드별 근거:** `CapturedRelevantAttributes`만 **반드시 깊은 복사** — 컨테이너가 내부 `List`를 들고 있어 공유하면 대상별 target 캡처가 서로의 컨테이너를 덮어쓴다(D14가 경고한 그 버그). `Definition`은 불변 정의라 공유(D2). `Executions`는 per-apply 상태 없는 로직이라 공유(D7 — 인스턴스별 상태가 생기면(D8이 SerializeReference로 가면) 복사로 전환). `Modifiers`는 원소가 불변 struct라 지금은 공유해도 무해하지만 **리스트 복사**를 택함 — 적용 시점 magnitude 재평가(유보 항목)가 들어오면 복사본이 자기 리스트에 재계산 결과를 담아야 하고, 값 struct라 복사 비용이 거의 0이라 그 haz ard를 미리 없앤다. **거부:** ①은 불변·무상태까지 깊은 복사라 낭비, ②는 컨테이너 공유로 버그, ③은 source를 불필요하게 재캡처하고 execution을 재resolve해 UE copy 생성자 의미와 어긋남, ④는 magnitude 재평가 도입 시 공유 리스트를 교체해 형제 clone을 오염. **결과·트레이드오프:** `GameplayEffectSpec`에 두 생성 경로(빌드=`Initialize`, 복사=복사 생성자)가 생긴다 — 필드가 추가되면 두 경로 모두 손봐야 한다(UE도 "any state must be handled in copy/move ctor" 주석으로 같은 부담 명시). **⚠ 관측 변화 0** — `Clone()`을 만들었을 뿐 `ApplyGameplayEffectSpecToSelf`에서 아직 호출하지 않는다(다음 단계 배선). **(UE 대응)** `FGameplayEffectSpec(const FGameplayEffectSpec&)` — Def 포인터 공유 + 값 멤버(Modifiers TArray·CapturedRelevantAttributes) 깊은 복사 채택 |
