# [아카이브됨 2026-07-12] item — 결정 기록

> **이 문서는 백지화되어 아카이브된 이전 결정 기록이다. 현행 결정이 아니다.**
> 2026-07-12 유저 결정으로 아이템 시스템 규약을 백지화(사유는 [HARNESS 아카이브](HARNESS.archived-2026-07-12.md) 상단 참조). D1~D12는 역사적 참조로만 보존 — 구속력 없음. 새 설계에서 되살릴 결정은 개별적으로 새 decisions.md에 옮겨 재정당화한다.

---

# item — 결정 기록

> 트레이드오프가 있었던 설계 결정의 상세(맥락·대안·근거·결과·재평가 트리거). 품질 기준은 일반 [HARNESS.md](../../../HARNESS.md) §8.
> **ID(`D#`)는 feature 내 고유·불변**이며 progress 인덱스·worklog·`다음 작업`이 이 코드로 이 표를 가리킨다. progress엔 요지 인덱스만 두고, **해당 영역을 건들 때 이 파일을 연다.**

| ID | 날짜 | 결정 | 대안 | 채택 사유 |
|---|---|---|---|---|
| D1 | 2026-07-04 | feature 전용 하네스/설계 문서를 `feature/<id>/` 폴더에 progress와 분리 배치 | 일반 HARNESS.md에 통합 | 아이템 시스템 고유의 불변 조건·로드맵이 방대해 feature 스코프로 격리, 프로세스 규약(일반 HARNESS)과 구현 규약(feature HARNESS)을 분리 |
| D2 | 2026-07-04 | **설계 권위: Item HARNESS 우선.** 상속 금지·POCO·모듈 조합(INV-2/3/4)이 이 시스템의 진실 | 아키텍처 문서(`item-equipment.md`)·기존 상속 기반 코드를 진실로 유지 | 포트폴리오상 "코드 0줄로 신규 효과 추가"(HARNESS §7 S9)를 증명하는 모듈 조합 설계가 핵심 산출물. 상속 구조로는 그 확장성 데모가 성립하지 않음 |
| D3 | 2026-07-04 | **장착 부위(slot)는 Module이 아니라 필드**로 둔다 | Requirement/Equippable Module로 분리 | 부위는 행동이 아니라 아이템당 하나로 고정된 정적 분류값(=주소)이라 Module의 재사용·조합 조건(INV-10)에 해당 안 됨. enum 하나를 위한 Module 축 신설은 북극성(트리거 전 복잡화 금지) 위반. **뒤집는 트리거**: 한 아이템이 복수 부위 동시 점유(양손무기 등) → `SlotType[]` 필드 확장 먼저, facet은 그 다음. ※ slot의 소속 위치는 아래 D4(category)로 정련됨 |
| D4 | 2026-07-04 | **아이템 종류(category)는 `ItemData` 서브클래스로 분리**: `EquipItem`/`ConsumeItem`(중요 아이템은 능력 없는 기본 or `IKeyItem` 마커). 각 서브클래스가 능력 인터페이스(`IEquippable`/`IConsumable`) 구현. **slot은 base `ItemData`에서 빠져 `EquipItem`(IEquippable) 필드로 이동** — 소비·중요 아이템은 slot을 갖지 않음. base엔 공통(uid/displayName/icon/prefab)만 | ① 전역 `type` enum + 조건부 필드 ② 능력 facet 조합(`EquippableFacet`/`ConsumableFacet`) | 종류는 소수·배타적이고 데이터 shape가 근본적으로 다른 구조 구분(장비=slot+스탯, 소비=use효과)이라 서브클래스가 자연스러움. `ItemData`가 이미 abstract라 서브타입 전제. 시스템은 구체 타입이 아닌 능력 인터페이스로 판별(INV-11). **INV-2 판단**: INV-2가 금지하는 건 "아이템별(FireSword식) 행동 상속"이지 카테고리 구조 상속이 아님 — 위반 아님. **경계선(INV-2 유지)**: 서브클래스는 종류까지만. 그 아래 아이템별 상속(`SwordItem`/`GunItem`/`FireSwordItem`) 금지 — 무기 vs 총 = Strategy(S2/S5), 행동 변주 = `modules` 조합. 전용 서브클래스 3개 초과 시 중단·보고(INV-10 규칙 준용) |
| D5 | 2026-07-04 | **능력 인터페이스는 그 능력을 실제로 소비하는 시스템이 있을 때만 정의**한다. 지금은 `IEquippable`(EquipmentComponent이 `Equip(IEquippable)` 경계에서 소비)만 노출. `IConsumable`/`IKeyItem`은 사용 처리기 등장 시 추가 | 종류별 인터페이스(`IEquippable`/`IConsumable`/`IKeyItem`)를 SO에 처음부터 전부 부착 | 종류당 클래스가 1:1·배타적이라 `is EquipItem` == `is IEquippable`, 지금 인터페이스의 다형성 이득은 0 → 전부 미리 붙이는 건 북극성 위반. 단 EquipmentComponent이 임의 `ItemData`를 받아 장착 가능성을 묻는 **시스템 경계 한 곳**에서는 능력으로 받는 게 INV-11이고 비용은 프로퍼티 한 줄. 규칙: "인터페이스는 그 능력을 읽는 시스템이 있을 때만 판다" (YAGNI + INV-11 동시 만족) |
| D6 | 2026-07-05 | **아이템 전용 자유형 로직은 `ItemRuntime`(별개 클래스, 멤버·로직 자유)로 두고 `ItemInstance`가 합성(멤버)으로 보유**. `ItemInstance`/`ItemData` 상속 아님. SO는 Runtime **타입만 지정**, 인스턴스 생성 시 **매 인스턴스 새 객체 생성**(`CreateState()` 미러링). 기본값 null. **구현은 첫 트리거 아이템까지 보류(현재 트리거 0)** | ① `ItemInstance`/`ItemBehaviour` 상속 서브클래스를 인스펙터에서 선택(INV-2 위반) ② 단일 아이템 유니크 로직도 Module로 억지 표현(자유형·아이템 전체 스코프 로직엔 의미 왜곡) | 재사용 안 되는 유니크 로직을 모듈 경계로 강제 분해하면 구현이 오히려 어려워짐(INV-10 금지 안티패턴 "유니크 로직을 억지로 Module 여러 개로 분해"). 자유형 클래스가 그 relief valve. **INV-2 유지**: 상속이 아니라 합성. **INV-1/5 유지**: SO 무상태(타입만), 가변 상태는 per-instance Runtime. **가드레일(INV-10)**: Module이 항상 기본·Runtime은 예외, 채택 시 "왜 Module로 안 되는지" 한 줄 근거, 누적 3개 초과 시 중단·보고. ※ 이 결정은 HARNESS INV-10 문구 개정을 전제 — progress 다음 작업 참조 |
| D7 | 2026-07-06 | **공격을 능력 인터페이스 `IWeaponAttack{ GameplayEffect DamageEffect; float Range }`로 선언**하고 (※ 2026-07-09 D11에서 `IMeleeAttack`로 개명 — 히트스캔 분리로 "무기 전체" 이름이 근접 전용 능력에 부정확해짐), 장비 시스템은 구체 `MeleeAttackModule`이 아니라 이 능력으로 무기의 공격을 요구·조회한다 (S1). | ① EquipmentComponent가 `module is MeleeAttackModule`로 구체 타입 하드코딩 ② 공격 능력을 `ItemData`/`EquipItem`에 직접 필드로 | INV-11(필수 요구는 인터페이스 능력으로)의 정확한 사례. 총기(S5)·특수공격(S8)이 생겨도 시스템은 이 인터페이스만 알면 되고, 발화 등 다른 모듈과 한 무기에 **조합**된다(모듈이 능력을 구현하는 단위라 자연스러움). D5 규칙("능력은 소비 시스템 있을 때만 판다") 충족 — 소비처=EquipmentComponent.TriggerAttack. **재평가 트리거**: 근접/원거리 판정 형태가 인터페이스로 안 담기게 갈라지면(투사체 등) 능력을 세분. |
| D8 | 2026-07-06 | **공격의 씬 판정·데미지 적용은 EquipmentComponent(시스템)가 호스팅**한다: 모듈은 데미지 GE·사거리라는 "결정"만 주고, `Physics.OverlapSphere` 전방 판정 + 각 타겟 `AbilitySystemComponent.ApplyGameplayEffectToSelf(damageEffect)`(대상이 자신에게 Instant 데미지 적용)로 실행. **대상 적용용 신규 ASC API는 만들지 않음.** | ① 모듈이 직접 오버랩·적용(INV-3 위반) ② `ItemBehaviour`가 실행(INV-4 위반) ③ ASC에 `ApplyGameplayEffectToTarget` 신규 추가 | INV-3/4 준수(씬 접근은 시스템/Behaviour, 그중 로직 없는 Behaviour 대신 시스템). `ApplyGameplayEffectToSelf`를 **대상 ASC에** 부르면 "대상이 자신에게 효과 적용"이라는 GAS 의미와도 맞아 신규 API가 불필요(북극성). **재평가 트리거**: 공격 소스가 무기 외로 늘거나 콤보/타이밍이 필요(S6)해지면 공격 실행을 EquipmentComponent에서 별도 전투/공격 드라이버로 분리. 지금은 최소로 EquipmentComponent가 겸함. |
| D10 | 2026-07-07 | **장착 표현(프리팹 소켓 소환)을 전투와 함께 `EquipmentComponent`가 겸한다**(별도 컴포넌트로 쪼개지 않음). 부위별 소켓 맵(`SlotType→Transform`) 필드를 보유하고 `Equip`/`Unequip` 안에서 `ItemData.prefab`을 소켓에 소환/파괴, `ItemBehaviour.Bind`로 인스턴스 연결. `ItemBehaviour`는 **표현 앵커(구체, 로직 0 — INV-4)**로 `ItemInstance` 참조+`Bind`만. 프리팹이 `ItemBehaviour`를 보유 → 소환 후 `GetComponent().Bind`. 실동작 불가(MonoBehaviour `new` 불가)한 `ItemData.CreateBehaviourRuntime` 팩토리는 제거. | ① **별도 `EquipmentVisual` 컴포넌트로 분리**(OnEquipped 구독) — 관심사 분리 우위지만 **한 개념(장비)에 컴포넌트 2개 → 발견성/헷갈림 비용**. 세션 중 먼저 구현했으나 유저 재검토로 기각·삭제 ② `CreateBehaviourRuntime` 팩토리 유지(MonoBehaviour `new` 못 해 실동작 X) ③ `ItemBehaviour` 미도입(목표가 Instance↔Behaviour 연결이라 기각) | **유저 결정(2차 질문): "장비=한 개념, 두 컴포넌트 분리는 헷갈린다."** 발견성 우위를 택함. INV-3 유지(씬 접근 주체가 Module이 아니라 시스템 컴포넌트라 허용), INV-4 유지(Behaviour 로직 0). `ItemBehaviour`는 목표가 명시한 Instance↔Behaviour 연결의 앵커라 유지(구체 1개, 파생은 두 번째 사용처에서). **D2 취지 정정(유저 2026-07-07):** equipment D2 "PlayerCharacter 미의존"의 실제 취지는 '적 재사용'이 아니라 **장비 상태를 캐릭터 사망·씬 전환에도 유지**하려는 것. **재평가 트리거**: 표현 로직이 커지면(애님 이벤트·다중 부착·풀링) 표현 전담 컴포넌트로 분리 / 소켓 규칙이 아이템별로 갈리면 소켓 맵을 ItemData로 이동. |
| D11 | 2026-07-09 | **총기(S5)는 투사체가 아니라 히트스캔**으로 구현: `GunAttackModule`(무상태) + 능력 인터페이스 `IHitscanAttack{ DamageEffect; Range }` 신설, `EquipmentComponent.TriggerAttack`이 능력 보유 여부로 판정 방식 분기(히트스캔=전방 Raycast 최근접 1기 / 근접=오버랩 전원). 능력 조회는 제네릭 `TryGetAbility<T>` 하나로 통일. 기존 `IWeaponAttack`은 **`IMeleeAttack`로 개명**(유저 지적 — 총 등장으로 "무기 전체" 이름이 근접 전용에 부정확). | ① 투사체(탄 프리팹 Instantiate + 탄 MonoBehaviour 이동·충돌) ② 기존 `IWeaponAttack`에 판정 형태 enum 추가 ③ 구체 모듈 타입 분기(INV-11 위반) | S5의 증명 목표는 "근접과 총이 같은 모듈 파이프라인에서 굴러간다"이며 히트스캔이 최소 코드로 충족(북극성). 투사체는 표현 비용만 크고 아키텍처 주장에 기여 없음 — 게임 필이 요구하면 그때 표현 레이어로 추가. 능력 분리는 D7의 재평가 트리거("판정 형태가 갈라지면 능력을 세분") 발동 사례 — enum 방식은 시스템이 형태별 분기를 결국 갖는 건 동일하면서 인터페이스 의미만 흐려 기각. **재평가 트리거**: 탄속·탄도·관통이 게임플레이 요구로 등장하면 투사체 경로 추가(히트스캔과 병존). |
| D12 | 2026-07-09 | **공격 실행을 `WeaponAttackComponent`(전투 드라이버)로 분리** — `EquipmentComponent`는 장착 상태·소켓 표현만(장비 도메인), 공격 판정·GE 적용(SwingMelee/FireHitscan/TryGetAbility)은 새 컴포넌트가 담당. 경계는 `EquipmentComponent.TryGetEquipped(slot)` 조회 API 하나. `PlayerCharacter.Attack()` → `WeaponAttackComponent.TriggerAttack()`. | ① 현행 유지(EquipmentComponent가 공격 겸함 — D8) ② 공격을 PlayerCharacter로(캐릭터 클래스 비대 + 적 재사용 불가) | 유저 지적("Equip 컴포넌트가 공격까지 하는 건 책임 분리 위반"). D8이 예고한 재평가 트리거가 히트스캔 분기 추가(D11)로 실질 발동 — 판정 방식이 2개가 되며 공격 실행부가 커져 장비 상태 관리와 응집도가 갈라짐. D10(표현은 겸함)과의 구분: 소켓 소환은 장착의 직접 결과(장비 도메인)라 잔류, 공격은 입력 구동 전투 행위라 분리. **재평가 트리거**: 콤보·타이밍(S6) 도입 시 이 드라이버가 무브셋 실행기로 확장(또는 재분리). |
| D9 | 2026-07-06 | **모듈 lifecycle이 자기 상태를 동반**한다: `OnEquip/OnUnEquip(ModuleContext, IModuleState)`, 순회·상태 정렬은 `ItemInstance`가 담당(상태 배열 캡슐화), `IModuleState→구체` 캐스트는 `ItemModule<TState>` 제네릭 베이스 1곳. `StatModifierModule`이 장착 GE 회수 핸들을 `StatModifierState`(per-item)에 보유. | ① 회수 핸들을 EquipmentComponent가 슬롯별로 추적(모듈이 아닌 시스템이 per-item 상태 보유) ② 모듈을 무상태로 두고 핸들을 static/외부에 | INV-5(per-item 상태는 ItemInstance 소유, 캐스트 1곳)의 정확한 준수. 장착 시 스탯 기여처럼 **적용↔회수가 짝인 효과**는 모듈이 자기 상태를 봐야 자연스러움. `GetState<T>(i)`(호출 단위 접근)와 달리 lifecycle은 상태를 주입받는 형태로 통일. **트리거로 착수:** 유저 요구(장착 시 이동속도 −5) → 로드맵 S3/S4를 부분 선착수. Tick은 소비자 생길 때 같은 방식 확장(현재 미변경). |
